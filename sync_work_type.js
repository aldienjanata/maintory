import { createClient } from '@supabase/supabase-js'; import fs from 'fs'; const env = fs.readFileSync('.env', 'utf8'); const sb = createClient(env.match(/VITE_SUPABASE_URL=(.*)/)[1], env.match(/VITE_SUPABASE_ANON_KEY=(.*)/)[1]); async function run() { const { data } = await sb.from('technician_schedules').select('id, work_type'); for (const s of data) { await sb.from('dispatches').update({ work_type: s.work_type }).eq('schedule_id', s.id); } console.log('Done synced!'); } run();
